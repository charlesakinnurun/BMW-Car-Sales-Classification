CREATE TABLE bmw(
    model VARCHAR(255),
    year INT,
    region VARCHAR(255),
    color VARCHAR(255),
    fuel_type VARCHAR(255),
    transmission VARCHAR(255),
    engine_size_l FLOAT,
    mileage_km INT,
    price_usd INT, 
    sales_volume INT,
    sales_classification VARCHAR(255)
);